xplr.config.modes.builtin.default.key_bindings.on_key["o"] = {
	help = "open focused",
	messages = {
		"PrintFocusedAndQuit"
	}
}
xplr.config.modes.builtin.default.key_bindings.on_key["O"] = {
	help = "open selected",
	messages = {
		"PrintSelectedAndQuit"
	}
}

